import { ExperienceLayout } from '@/components/ExperienceLayout';

export const experience = {
  title: 'GvCloud secure',
  description: `GvCloud Secure is a digital transformation and engineering company offering software development services across the USA, UK, Australia, India, and Singapore. With over a decade of experience, they specialize in custom web and mobile application development, aiming to bridge the gap between strategy and execution. Their services include software consulting, enterprise solutions, and 24/7 maintenance and support, all delivered by a dedicated team of professionals.`,
  date: 'Aug 2023 – Nov 2023', // Added date field
  details: [
    {
      role: 'frontend Developer intern',
      company: 'Gv cloud secure',
      location: 'Noida',
      responsibilities: [
        'I have worked on web based product thereby developed a deep understanding of all aspects of product development such as identifying appropriate user requirements, designing a great user experience and building appropriate models along with the relevant UI components and backend design.',
        ' implemented new features built components using React.js and Tailwind CSS.',
        '• Reduced the bounce rate by 5% by creating a user-friendly interface that was easy to navigate and use.',
        'Improved the loading speed by 10% by using lightweight UI components.',
      ],
    },
  ],
};

export const metadata = {
  title: experience.title,
  description: experience.description,
};

export default function ExperiencePage(props) {
  return (
    <ExperienceLayout experience={experience} {...props}>

      <section>
        <h2>Experience Details</h2>
        <p><strong>Date:</strong> {experience.date}</p> {/* Added date display */}
        <p>{experience.description}</p>
        <h3>Responsibilities</h3>
        <ul>
          {experience.details[0].responsibilities.map((responsibility, index) => (
            <li key={index}>{responsibility}</li>
          ))}
        </ul>
      </section>
    </ExperienceLayout>

);
}
